$(document).ready(() => {
    $('.slider').slick({
        prevArrow: $('.slick-prev'),
        nextArrow: $('.slick-next'),
        autoplay: true,
        autoplaySpeed: 3000,
        speed: 1000,
        cssEase: 'linear',
    });
});
